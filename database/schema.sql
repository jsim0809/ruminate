CREATE TABLE reviews (
  key                    text,
  location               text,
  metrics                text,
  review_text            text,
  review_date            smallint,
  review_metrics         text,
  review_firstname       text,
  review_lastname        text,
  review_city            text,
  review_tags            smallint
);